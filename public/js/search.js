(() => { const input = document.querySelector('#site-search'); const results = [...document.querySelectorAll('[data-search]')]; const empty = document.querySelector('[data-search-empty]'); if (!input || !results.length) return; input.addEventListener('input', () => { const query = input.value.trim().toLowerCase(); let visible = 0; results.forEach((result) => { const matches = !query || result.dataset.search.includes(query); result.hidden = !matches; if (matches) visible += 1; }); if (empty) empty.hidden = visible !== 0; }); })();